%h1 Messages' List:
%table
  %tr
    %th User
    %th Body
    %th Link to message

  - @messages.each do |message|
    %tr
      %td User Placeholder
      %td= message.body
      %td= link_to 'Show', message_path(message)
      %td= link_to 'Edit', edit_message_path(message)
      %td= link_to 'Destroy', message_path(message), 
        method: :delete, 
        data: {confirm: 'Are you sure you want to destroy this message?',
        class: 'btn btn-danger'}
